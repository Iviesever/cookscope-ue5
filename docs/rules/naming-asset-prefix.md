# `naming.asset-prefix`

Requires each configured Asset Class to use its configured object-name prefix. `classPrefixes` maps full class paths to prefixes. Scope include/exclude/exception rules apply before evaluation.

The default config checks CookScope fixtures plus common Texture, Static Mesh, Skeletal Mesh, and SoundWave prefixes. `BadName` is the intentional failing sample.
